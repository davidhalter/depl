deploy:
    - django:
        id: django
        url: 127.0.0.1
        port: 80
    - redis:
        id: id
    - postgresql:
        id: id

hosts:
    - <user@example:port>:
        password: null
        id: id

pool:
    - <name>:
        hosts: [<id>]
        deploy: [<id>]

extends:
    - <file>
