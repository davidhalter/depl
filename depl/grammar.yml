deploy:
    - django:
        id: django
        url: 127.0.0.1
        port: 80
    - redis:
        id: redis
    - postgresql:
        id: postgresql

hosts:
    - <user@example:port>:
        password: null
        id: <user@example:port>

pools:
    - <name>:
        hosts: [<id>]
        deploy: [<id>]

extends:
    - <file>
