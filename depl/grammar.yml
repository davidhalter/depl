deploy:
    - python:
        id: python
        url: localhost
        port: 80
    - django:
        id: django
        url: localhost
        port: 80
    - redis:
        id: redis
    - postgresql:
        id: postgresql

hosts:
    - <user@example:port>:
        password: null
        id: null  # actually the same as the host identifier

pools:
    - <name>:
        hosts: [<id>]
        deploy: [<id>]

extends:
    - <file>
