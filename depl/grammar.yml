deploy:
    - python:
        id: python
        url: localhost
        port: 80
        wsgi: null
    - django:
        id: django
        settings: settings
        url: localhost
        port: 80
        wsgi: null # project/wsgi.py will be searched.
    - redis:
        id: redis
    - postgresql:
        id: postgresql

hosts:
    - <user@example:port>:
        password: null
        id: null  # actually the same as the host identifier

pools:
    - <name>:
        hosts: [<id>]
        deploy: [<id>]

extends:
    - <file>
