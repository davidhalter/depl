deploy:
    - django:
        id: django
        url: 127.0.0.1
        port: 80
    - redis:
        id: id
    - postgresql:
        id: id

server:
    - <user@example:port>:
        password: null
        id: id

pool:
    <name>:
        server: [id]
        deploy: [id]
